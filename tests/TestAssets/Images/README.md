# AtomPix Test Image Assets

This directory stores deterministic source images used by visual output artifact tests.

Files are generated by `VisualOutputArtifactTests` when missing. Tests copy or read these assets and write processed results to `tests/TestOutputs/Images/`.

Do not edit generated baseline assets unless the expected visual sample set intentionally changes.
