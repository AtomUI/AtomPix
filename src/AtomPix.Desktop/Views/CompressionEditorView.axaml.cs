namespace AtomPix.Desktop.Views;

using Avalonia.Controls;
using Avalonia.Markup.Xaml;

public sealed partial class CompressionEditorView : UserControl
{
    public CompressionEditorView() => AvaloniaXamlLoader.Load(this);
}
