namespace AtomPix.Desktop.Views;

using Avalonia.Controls;
using Avalonia.Markup.Xaml;

public sealed partial class ResizeEditorView : UserControl
{
    public ResizeEditorView() => AvaloniaXamlLoader.Load(this);
}
